{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "nuxt": "3.0.0"
  },
  "dependencies": {
    "@types/marked": "^4.0.8",
    "gh-pages": "^4.0.0",
    "highlight.js": "^11.7.0",
    "marked": "^4.2.5"
  }
}
